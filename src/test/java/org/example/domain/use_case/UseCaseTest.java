package org.example.domain.use_case;

public class UseCaseTest {

    // for unit test we would need repository mock object
    // for integration test we can use guice inject
    // its late and I cant be bothered to copy-paste repository test code here because its same functionality
    // real life would be different case as there would be more differences and test would be needed, just imagine there are tests here
}
